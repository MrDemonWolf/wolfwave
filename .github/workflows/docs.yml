name: Deploy Docs

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2

      - uses: actions/configure-pages@1f0c5cde4bc74cd7e1254d0cb4de8d49e9068c7d # v4
        id: pages

      - name: Propagate base path (optional)
        run: echo "NEXT_PUBLIC_BASE_PATH=${{ steps.pages.outputs.base_url }}" >> "$GITHUB_ENV"

      - name: Install deps
        run: bun install --frozen-lockfile

      - name: Build docs
        run: bun run build --filter docs

      - name: Verify build output
        run: |
          if [ ! -d apps/docs/out ] || [ -z "$(ls -A apps/docs/out)" ]; then
            echo "::error::apps/docs/out is missing or empty"
            exit 1
          fi

      - uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b # v4
        with:
          path: apps/docs/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
